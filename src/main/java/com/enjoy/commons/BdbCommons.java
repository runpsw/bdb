package com.enjoy.commons;

/**
 * @author Sven
 * @date 2021-01-10
 *
 * berkeley db 的操作
 */
public class BdbCommons {

    // 初始化指定的读写表的bdb

    // 初始化保存offset的db

    // 给新读取和写入的表分别建立一个读写的offset read offset，write offset

    // 初始话offset
    /*
    首先判断表是否存在
    不存在就创建读写的bdb表，并且调用 根据指定offset 更新bdb表中的offset
    offset 初始化完成
     */

    // offset 更新


    // 获取offset

}
